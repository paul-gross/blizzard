# The packaged ideation graph

Conventions for the `prompts/` tree are owned by the
[prompt-authoring README](../advanced-development-workflow/README.md) — read it before adding or editing a node
prompt.

This graph carries no frozen plan artifact to correct against, unlike `garden-routine`'s — it was authored directly as
a packaged graph, so it has no corrections section of its own.
