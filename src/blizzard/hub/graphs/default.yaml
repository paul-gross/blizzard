# The default workflow graph — build -> review -> deliver.
#
# The autonomous-baseline shape: the canonical review-fail cycle with NO
# human gate. Promoted from the P6 walking-skeleton `build -> deliver` slice to the
# full MVP graph (milestone:mvp): build hands to a cold-eyes review node, review
# either passes to delivery or fails the work back into build carrying its findings
# asset (design/workflow-engine.md review node). Insert an `approve-gate` human node
# ahead of `deliver` (design/hub/sample-graph.yaml) to dial HITL back up — graphs are
# immutable, so that is a re-mint, not an edit.
#
# Conforms to the canonical authoring schema:
# fused choice/edge entries, prompts as file references inlined at mint,
# retries escaping to escalation, the review-fail edge carrying the findings
# asset + its prompt_addendum back into build, and delivery as a
# hub-executed node whose machinery outcomes (landed -> done, conflict -> entry) are
# accepted by default. Operational metadata (enabled, auto_migrate,
# migration_target) is NOT authored here — it is set after mint via PATCH /graphs/{id}.

name: default-delivery
entry: build

nodes:
  build:
    executor: runner
    prompt: ./prompts/build.md
    checks:
      - mise run lint
      - mise run test
    judgement:
      prompt: ./prompts/build.judgement.md
      choices:
        pass:
          description: The work meets this node's criteria and the checks are green.
          to: review
        fail:
          description: The work does not meet the criteria; the failure output is attached.
          to: build
          prompt_addendum: ./prompts/build.from-build.md
    retries:
      max: 2
      exhausted: escalate

  review:
    executor: runner
    prompt: ./prompts/review.md
    session: fresh  # cold eyes for review; the default is resume
    produces:
      - review-findings  # asset artifact — carried back into build on a fail
    judgement:
      prompt: ./prompts/review.judgement.md
      choices:
        pass:
          description: The submitted work passes review and is ready to deliver.
          to: deliver
        fail:
          description: Review found blocking issues, recorded in the review-findings asset.
          to: build  # THE LOOP: the findings asset rides this edge back into build
          prompt_addendum: ./prompts/build.from-review.md
    retries:
      max: 2
      exhausted: escalate

  deliver:
    executor: hub
    mode: merge-to-main
    # No judgement: accept the deliver machinery defaults — landed -> done (the
    # reserved terminal) and conflict -> entry (build) in the MVP. The
    # conventional cross-graph `merge` graph arrives with epic:migration.
