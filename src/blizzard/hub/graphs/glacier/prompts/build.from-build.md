# Build — re-entry after a failed attempt

You are re-entering the **build** node after the previous attempt did not pass: a phase was incomplete or the checks were red. The failure output is attached below. Address every point in phase order, re-run the checks until they are green, and commit the fix before you declare done again.
