You are re-entering this node after a pre-push rebase that needed semantic conflict choices or materially reshaped the
change. The rebase rewrote the branches, so the previous attempt's view of what each repo carries is stale. Revalidate
the rebased result here, concentrating on the behavior the conflict resolutions touched and on anything the rebase
disturbed. The `pre-push-summary` asset in this envelope records each conflict and its chosen resolution.
