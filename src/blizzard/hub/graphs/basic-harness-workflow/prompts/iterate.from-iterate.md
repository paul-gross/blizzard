# Iterate — re-entry after a failed iterate attempt

This re-entry to the `iterate` node follows an attempt recorded `fail` for not yet fully answering review's findings.
The failed attempt's own output rides along: address every point it raises before declaring done again.
