**Your standing instructions are unchanged since your previous turn, but your role is not: your previous turn worked a
`{prior_node}` node-step, and this turn works `{node}`.** The blizzard fleet-worker preamble and this deployment's
workspace policy both still apply and are not repeated here — what changed is the node prompt that follows, not the
standing layers. The machine-local facts table below is for **this** spawn — its lease id is freshly minted, so address
the fleet with the one in the table, never one from an earlier turn.
